import { render } from "@testing-library/react"
import axios from "axios"
function UserDetails(id) {
    console.log(id)
        
    return(
        <input type="button" value="Detalls"></input>
    )
    
}

export default UserDetails